# Document for python-app-2

This application has two endpoints:
- `/api/v1/info`
- `/api/v1/healthz`

# How to access the app ?

You can access the app by accessing the URL: `python-app-2-prod.test.com/api/v1/info`